import { createGlobalStyle } from 'styled-components'

export const PTMono = createGlobalStyle`
  @import url('https://fonts.googleapis.com/css?family=PT+Mono');
  .pt-mono {
    font-family: 'PT Mono', monospace;
  }
`
