import { Page, Section } from '@layouts'
import { Router } from '@components/Router'
import { useRouteData } from 'react-static'
import { convert } from '@components/System/hrmr'

export default Page

export const title = 'Session Formats'

export const meta = {
  authors: ['foo', 'bar'],
  layout: Page,
  className: 'mw7',
}

# What will I learn at IPFS Camp?

<div>{useRouteData().formats.map(format => convert(format.contents))}</div>
