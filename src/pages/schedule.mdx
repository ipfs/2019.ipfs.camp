import { Page } from '@layouts'
import { Schedule } from '@components/Schedule'

export default Page

export const title = 'Schedule'

export const meta = {
  authors: ['foo', 'bar'],
  layout: Page,
  className: 'mw7',
}

# What should I expect from IPFS Camp?

IPFS Camp is a mix of talks, workshops, deep dives, poster sessions, informal networking and shared experiences.

Our full schedule for IPFS Camp 2019 is to be confirmed, but here's a rough guide to what to expect.

<Schedule />
